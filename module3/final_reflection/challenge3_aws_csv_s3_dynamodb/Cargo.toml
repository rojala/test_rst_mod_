[package]
name = "aws_csv_s3_dynamodb"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
aws-config = "1"
aws-sdk-s3 = "1"
aws-sdk-dynamodb = "1"
csv = "1.3"
serde = { version = "1.0", features = ["derive"] }
